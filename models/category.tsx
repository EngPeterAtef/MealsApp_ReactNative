/* eslint-disable prettier/prettier */
class Category {
    public constructor(public id: string, public title: string, public color: string) {
        this.id = id;
        this.title = title;
        this.color = color;
    }
}

export default Category;
